# Backend API base URL (FastAPI screening pipeline)
BACKEND_URL=http://127.0.0.1:8000

# Set true to skip API calls and load frontend/mock_data/mock_data.json
USE_MOCK_DATA=false

# Polling while a run is in progress
POLL_INTERVAL_SECONDS=5
POLL_TIMEOUT_SECONDS=900
